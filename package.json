{
  "name": "@wai-ri/monorepo",
  "private": true,
  "version": "0.0.99",
  "description": "util",
  "author": "ziloen",
  "license": "ISC",
  "scripts": {
    "test": "vitest --ui",
    "release": "bumpp package.json packages/*/package.json --commit --no-push && pnpm -r publish --access public",
    "lint": "eslint . --ignore-pattern '*.test.*' --fix",
    "typecheck": "vitest typecheck"
  },
  "devDependencies": {
    "@types/node": "^20.5.7",
    "@vitest/ui": "^0.34.3",
    "@ziloen/eslint-config": "^0.0.101",
    "@ziloen/eslint-config-format": "^0.0.101",
    "bumpp": "^9.2.0",
    "eslint": "^8.48.0",
    "jsdom": "^22.1.0",
    "typescript-lib-enhancer": "^1.0.12",
    "vite": "^4.4.9",
    "vitest": "^0.34.3"
  },
  "dependencies": {
    "typescript": "^5.2.2"
  },
  "eslintConfig": {
    "extends": [
      "@ziloen",
      "@ziloen/eslint-config-format"
    ],
    "root": true,
    "env": {
      "node": true,
      "browser": true,
      "es2022": true
    },
    "parserOptions": {
      "project": "./tsconfig.json"
    },
    "ignorePatterns": [
      ".*"
    ]
  }
}
