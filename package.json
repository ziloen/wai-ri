{
  "name": "@wai-ri/monorepo",
  "private": true,
  "version": "0.0.108",
  "description": "util",
  "author": "ziloen",
  "license": "ISC",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "test": "vitest --ui",
    "release": "pnpm build && bumpp package.json packages/*/package.json --commit --no-push && pnpm -r publish --access public",
    "lint": "eslint . --ignore-pattern '*.test.*' --fix",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "@vitest/ui": "^4.1.10",
    "@ziloen/eslint-config": "^0.1.82",
    "bumpp": "^11.1.0",
    "eslint": "^10.7.0",
    "jsdom": "^29.1.1",
    "ts-lib-enhance": "^1.0.24",
    "tsdown": "^0.22.9",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "typescript": "^6.0.2"
  }
}
